import { log, logSection } from '../utils/logger';

export function reportDashboard(): void {
  logSection('📊 Monitoring Surface', 'equity, drawdown, signal telemetry');
  log('info', 'Dashboard placeholder ready.', 'crypto cards • parity spreads • signals');
}
